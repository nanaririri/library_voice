# 인테리어 건축 라이브러리 (Vanilla JS + Vite)

기존 단일 `index.html` 파일을 유지보수 가능한 프론트엔드 프로젝트 구조로 리팩토링한 결과물입니다.
**디자인, 레이아웃, 텍스트, 이미지, 인터랙션, 기능은 원본과 동일**하며, 코드 구조만 분리했습니다.

## ① 최종 폴더 구조

```
project/
├── index.html          # HTML 뼈대 (header, stage, modal, listmap, footer)
├── package.json         # npm 설정 (dev/build 스크립트)
├── vite.config.js       # Vite 빌드 설정
├── .gitignore
├── src/
│   ├── css/
│   │   └── style.css    # 기존 <style> 태그 내용 그대로 분리
│   ├── js/
│   │   └── main.js      # 기존 <script> 태그 내용 그대로 분리
│   └── assets/
│       └── images/      # (현재 비어 있음 — 아래 "이미지 경로" 참고)
├── public/               # 정적 파일 폴더 (현재 비어 있음)
└── dist/                 # npm run build 실행 시 생성되는 배포용 결과물
```

## ② 각 파일의 역할

| 파일 | 역할 |
|---|---|
| `index.html` | 페이지의 정적 뼈대(header, stage 컨테이너, modal, listmap, footer)와 Tailwind CDN, Google Fonts, CSS/JS 연결 |
| `src/css/style.css` | 전체 디자인(색상 변수, 카드/버튼/모달/리스트맵 스타일, 애니메이션 등) |
| `src/js/main.js` | 데이터(DATA, COMPANY_OVERRIDES 등), 렌더링 로직(단계별 화면), 검색, 리스트맵, 모달, 히스토리(뒤로가기), 이벤트 위임 등 전체 인터랙션 |
| `package.json` | npm 스크립트(`dev`, `build`, `preview`) 및 Vite 의존성 정의 |
| `vite.config.js` | Vite 빌드 설정 (출력 폴더: `dist`) |

## ③ 실행 방법 (개발 서버)

```bash
npm install
npm run dev
```

터미널에 표시되는 로컬 주소(기본 `http://localhost:5173`)를 브라우저에서 열면 됩니다.

## ④ 빌드 방법 (배포용)

```bash
npm run build
```

`dist/` 폴더에 배포 가능한 정적 파일(HTML, 해시가 붙은 CSS/JS)이 생성됩니다.
결과물을 미리 확인하려면:

```bash
npm run preview
```

## ⑤ 기존 기능 중 변경된 부분

**기존 기능 변경 없음.**

- `<style>` 내용을 `src/css/style.css`로, `<script>` 내용을 `src/js/main.js`로 **바이트 단위로 동일하게** 옮겼습니다 (내용 자체는 한 글자도 수정하지 않았습니다).
- 클릭/탭/메뉴/팝업/모달/리스트맵/검색/뒤로가기(History API)/hover/애니메이션 등 모든 인터랙션은 `data-action` 속성 기반 이벤트 위임 방식으로 구현되어 있어 전역 함수에 의존하지 않으므로, `<script type="module">`로 옮겨도 동일하게 동작합니다. (인라인 `onclick=` 같은 속성은 원본에 없었습니다.)
- Tailwind CDN, Google Fonts 링크는 원본 그대로 `<head>`에 유지했습니다.
- `npm install`, `npm run dev`, `npm run build` 모두 정상 동작을 확인했습니다 (콘솔/빌드 오류 없음).

## ⑥ 이미지 경로

현재 프로젝트에서 사용하는 모든 이미지는 **GitHub의 외부 URL**(`https://raw.githubusercontent.com/nanaririri/ai/main/...`)로 연결되어 있습니다. 상대경로나 Base64 이미지는 없습니다.

발견된 이미지 목록:

- `logo.png` (헤더 로고)
- `burim_logo.png` (업체 로고 오버라이드)
- `yu01.jpg`, `yu02.png`, `yu03.jpg`, `yu04.jpg` (서브 카테고리 이미지)
- `ago02.jpg` (서브 카테고리 이미지)
- `material.jpg`, `glass.png` (서브 카테고리 이미지)
- `01.png` (제품 이미지 오버라이드)

요청하신 대로 **모든 GitHub 이미지 URL을 그대로 유지**했으며, 다운로드하거나 다른 이미지로 교체하지 않았습니다. 현재 URL들은 모두 정상적으로 접근 가능한 형태(raw.githubusercontent.com)이므로 별도 조치가 필요하지 않습니다. 다만 GitHub 저장소가 비공개로 전환되거나 파일이 삭제되면 이미지가 깨질 수 있으니, 장기적으로는 `src/assets/images/`로 이미지를 내려받아 로컬 경로로 전환하는 것을 고려해볼 수 있습니다 (현재는 요청에 따라 변경하지 않았습니다).

## PC 버전 업체 레이어팝업 가로 확장 (9차 업데이트)

PC(데스크톱, 1024px 이상) 화면에서 업체 상세 레이어팝업의 가로 폭을 더 넓혔습니다.

- `lg:max-w-3xl`(약 768px) → `lg:max-w-5xl`(약 1024px)
- 모바일·태블릿(1024px 미만) 폭은 기존과 동일하게 유지했습니다.
- 수정 파일: `index.html` (`#modal-panel` 클래스 한 줄만 변경)
- Tailwind 유틸리티의 실제 CSS 값을 주입해 렌더링 폭이 약 768px → 약 1024px로 확장된 것을 직접 측정해 확인했습니다.

## 취급 제품 텍스트 스타일 · 모달 스크롤 구조 개선 (8차 업데이트)

- **취급 제품 정보 텍스트**: 글자 크기를 `text-[11px]` → `text-sm`(14px)로 키우고, 값 텍스트 색상을 옅은 회색(`--ink-soft`)에서 가장 진한 잉크 색상(`--ink`)으로 바꿔 더 또렷하게 보이도록 했습니다. 항목 라벨(용도/소재/색상/규격/무게)은 기존처럼 작은 보조색으로 유지해 라벨과 값이 구분되게 했습니다.
- **모달 스크롤 구조**: 모달 패널을 세로 flex 레이아웃으로 바꿔 `#modal-body`(업체 정보·취급 제품)만 세로 스크롤되고, `#modal-footer`(카탈로그 다운로드 버튼)는 하단에 고정되도록 했습니다.
  - 수정 파일: `index.html`(모달 패널 구조를 `#modal-body` + `#modal-footer`로 분리), `src/js/main.js`(다운로드 버튼을 `#modal-footer`에 별도로 렌더링)
  - Tailwind의 실제 CSS 유틸리티(`flex`, `flex-1`, `overflow-y-auto`, `shrink-0`)를 적용한 시뮬레이션 테스트로, 본문은 스크롤되고 하단 버튼은 위치가 고정되는지 직접 확인했습니다. (이 샌드박스는 네트워크 정책상 Tailwind CDN 자체는 못 불러오므로, 해당 유틸리티의 실제 CSS 규칙을 동일하게 주입해 검증했습니다.)

## 취급 제품 상세 스펙 추가 (7차 업데이트)

취급 제품 각 이미지 아래에 상세 정보를 추가했습니다.

- **용도** / **소재** / **색상** / **규격** / **무게** — 제품 이미지마다 5개 항목이 표시됩니다.
- 실제 사진이 있는 업체(부림테크, 유송타일 등)와 일반 예시 스와치 이미지를 쓰는 업체 모두 동일하게 표시되도록, 제품 배열의 순서(index) 기반으로 값을 생성하는 `productSpecs()` 헬퍼를 추가했습니다.
- 수정 파일: `src/js/main.js` (제품 목록 렌더링 부분에만 추가, 기존 이미지·레이아웃 구조는 그대로 유지)
- Playwright로 사진이 있는 업체/없는 업체 모두 5개 항목이 정상 표시되는지, 콘솔 에러가 없는지 확인했습니다.

## 업체 정보 · 취급 제품 좌우 레이아웃 (6차 업데이트)

업체 상세 모달에서 "업체 정보"와 "취급 제품"을 좌/우로 나란히 배치했습니다.

- 데스크톱(≥1024px, `lg` 브레이크포인트)에서는 좌측에 업체 정보, 우측에 취급 제품이 2단으로 표시됩니다.
- 모바일·태블릿(1024px 미만)에서는 좁은 화면에서 두 칸이 억지로 눌리지 않도록 기존처럼 세로로 쌓이는 레이아웃을 유지했습니다.
- 좌우 2단 레이아웃이 여유 있게 보이도록 모달 패널의 최대 너비를 데스크톱 기준으로 넓혔습니다(`sm:max-w-lg` → `lg:max-w-3xl`, 모바일/태블릿 너비는 기존과 동일).
- "카탈로그 다운로드" 버튼은 두 칸 전체 아래에 기존처럼 가로로 꽉 차게 유지했습니다.
- 수정 파일: `src/js/main.js`(모달 렌더링 구조), `index.html`(모달 패널 최대 너비) — 정보 항목 자체의 내용/문구는 변경하지 않았습니다.
- Playwright로 그리드 구조(업체 정보/취급 제품이 각각 별도 컬럼으로 존재하는지)와 콘솔 에러 여부를 확인했습니다. 다만 이 테스트 환경은 Tailwind CDN을 네트워크 정책상 불러올 수 없어 실제 픽셀 단위 시각 렌더링까지는 확인하지 못했습니다 — `npm run dev`로 실행해 직접 화면을 확인해 보시길 권장드립니다.

## 업체 정보 필드 추가 (5차 업데이트)

업체 상세 정보(모달)에 아래 항목을 추가했습니다.

- **업태**: 제조 / 유통 중 1
- **제조국**: 국산 / 중국 / 이태리 중 1
- **대표자**: 대표자명
- **대표전화**: `tel:` 링크로 연결해 모바일에서 탭하면 바로 전화 연결
- **E-mail**: `mailto:` 링크로 연결해 탭하면 바로 메일 작성

표시 위치는 기존 "대표제품" 항목 바로 아래이며, 다운로드되는 카탈로그 텍스트 파일에도 동일하게 포함했습니다.

- 수정 파일: `src/js/main.js` (`co()` 헬퍼 함수에 필드 생성 로직 추가, 모달/카탈로그 렌더링에 항목 추가)
- 전화번호는 02/031/032/041/051/053/062/063 등 실제 존재하는 지역번호 형식으로 생성해 `tel:` 링크가 유효하도록 했습니다.
- 데스크톱·모바일(터치) 환경 모두에서 전화 링크와 이메일 링크가 정상적으로 렌더링되는지 Playwright로 확인했습니다.

## 카테고리 추가 (4차 업데이트)

인테리어 대분류에 **시공** 중분류를 새로 추가했습니다. (건축 대분류에 이미 있는 "시공"과 이름은 같지만 별도 항목이며, 내부 id를 `installation`으로 구분해 건축의 `construction-service`와 충돌하지 않도록 했습니다.)

- 인테리어 > 시공 > 소분류: 가구시공, 주방시공, 바스시공, 오피스시공, 기본공사, 철거 (각 2개씩 예시 업체 배치, 총 12개 업체)
- 수정 파일: `src/js/main.js` (`DATA.interior.subs`에 항목 추가만 했고, 기존 카테고리·업체 데이터는 전혀 건드리지 않았습니다)
- Playwright로 인테리어 카테고리 목록에 "시공"이 정확히 추가됐는지, 6개 소분류 탭이 모두 뜨는지, 탭별로 업체가 정상 표시되는지, 콘솔 에러가 없는지 확인했습니다.

## 카테고리 구조 변경 (3차 업데이트)

첨부하신 대분류/중분류/소분류 이미지에 맞춰 전체 카테고리 체계를 재구성했습니다.

- 수정 파일: `src/js/main.js` (`DATA` 객체, 아이콘, 이미지 매핑), 다른 파일은 변경 없음
- 대분류: **인테리어**(11개 중분류) / **건축**(9개 중분류, 기존 "외장 건축 기자재"에서 이미지에 맞춰 명칭 변경)
- 인테리어 중분류: 원자재·부자재, 가구·리빙, 주방, 바스, 도어·중문, 바닥재, 석재, 벽장재, 창호, 커튼, 오피스
- 건축 중분류: 건축시공, 야외 바닥재, 외장재, 조형물, 야외가구, 건축설계, 모듈러하우스, 시공, 기타
- 각 중분류 하위 소분류는 이미지에 표기된 항목만 반영했고(빈 칸은 의미 없는 셀 병합으로 보고 제외), 각 소분류에는 최소 1개 이상의 예시 업체를 배치해 화면이 비어 보이지 않도록 했습니다.
- 기존에 등록되어 있던 실제 예시 업체(부림테크, 유송타일, 아주스톤, 포스코휴먼스, 일흥건영, 미가월패널, AGO, 92 scent, SITE LESS, 워크업 등)는 삭제하지 않고 의미가 가장 가까운 새 소분류로 재배치했습니다. (예: 일흥건영 → 건축 > 건축시공 > 흡음재, SITE LESS → 건축 > 건축설계, 워크업 → 건축 > 기타)
- 새로운 중분류(원자재·부자재, 가구·리빙 소품, 오피스, 커튼, 조형물, 건축설계, 모듈러하우스, 시공 등)에는 예시 업체를 새로 추가했습니다.
- **부수적으로 발견해 함께 고친 버그**: 카테고리 이미지 로딩 실패 시 아이콘으로 대체하는 `handleSubImgError` 함수가 `<script type="module">` 환경에서는 전역 스코프에 노출되지 않아, 이미지가 로드되지 않을 경우 콘솔 에러가 발생하며 대체 아이콘이 표시되지 않는 문제가 있었습니다(1차 구조 분리 작업 시 생긴 잠재적 버그). `window.handleSubImgError`로 명시적으로 등록해 수정했습니다.
- Playwright로 인테리어 11개·건축 9개 중분류가 이미지와 정확히 일치하는지, 소분류 필터 탭이 올바르게 뜨는지, 재배치된 업체(부림테크 등)가 검색에서 새 카테고리로 정상 노출되는지, 콘솔 에러가 없는지 모두 확인했습니다.

## 음성 검색 기능 (2차 업데이트)

기존 검색창(1단계 화면)에 **Web Speech API(SpeechRecognition) 기반 음성 검색**을 추가했습니다.

- 수정 파일: `src/js/main.js` (검색창 HTML 마크업 + 음성 인식 로직), `src/css/style.css` (마이크 버튼 스타일만 추가)
- 기존 검색 로직(`performSearch`)을 그대로 재사용하며, 별도의 검색 로직을 새로 만들지 않았습니다.
- 마이크 버튼은 검색창 안, 지우기(×) 버튼과 검색 버튼 사이에 추가되었고 최소 44×44px 터치 영역을 확보했습니다.
- `SpeechRecognition`/`webkitSpeechRecognition`을 지원하지 않는 브라우저에서는 마이크 버튼이 자동으로 숨김 처리되어 기존 검색 기능에 전혀 영향을 주지 않습니다.
- 언어는 `ko-KR`, `interimResults: true`로 설정되어 있어 말하는 동안 검색창 텍스트가 실시간으로 갱신됩니다. 최종 인식 결과가 나오면 기존 검색(`performSearch`)이 그대로 실행됩니다.
- 오류(권한 거부, 미지원, 무음, 네트워크 오류 등)는 기존 검색 결과 드롭다운(`#search-results`) 영역을 재사용해 안내 문구로 표시하며, 어떤 오류가 나도 기존 텍스트 검색은 정상 동작합니다.
- 접근성: 마이크 버튼에 `aria-label="음성 검색"`, `aria-pressed`를 추가했고, 화면에는 보이지 않는 `aria-live` 영역으로 상태 변화를 스크린리더에 안내합니다.
- 1단계 화면을 벗어나거나 `Esc`를 누르면 진행 중인 음성 인식을 자동으로 중단합니다.
- Playwright로 마이크 클릭 → 중간 결과 반영 → 최종 결과로 기존 검색 실행 → 오류 처리 → 미지원 브라우저 시 자동 숨김까지 모두 시뮬레이션하여 정상 동작을 확인했습니다.

## 기술 스택

- **Vanilla JavaScript** (프레임워크 없음 — 원본이 프레임워크 없이 구현되어 있어 그대로 유지)
- **Vite** — 개발 서버 + 프로덕션 빌드
- **Tailwind CSS (CDN)** — 원본과 동일하게 CDN 스크립트로 로드
