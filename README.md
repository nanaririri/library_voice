# 인테리어 건축 라이브러리 (Vanilla JS + Vite)

기존 단일 `index.html` 파일을 유지보수 가능한 프론트엔드 프로젝트 구조로 리팩토링한 결과물입니다.
**디자인, 레이아웃, 텍스트, 이미지, 인터랙션, 기능은 원본과 동일**하며, 코드 구조만 분리했습니다.

## ① 최종 폴더 구조

```
project/
├── index.html          # HTML 뼈대 (header, stage, modal, listmap, footer)
├── package.json         # npm 설정 (dev/build 스크립트)
├── vite.config.js       # Vite 빌드 설정
├── .gitignore
├── src/
│   ├── css/
│   │   └── style.css    # 기존 <style> 태그 내용 그대로 분리
│   ├── js/
│   │   └── main.js      # 기존 <script> 태그 내용 그대로 분리
│   └── assets/
│       └── images/      # (현재 비어 있음 — 아래 "이미지 경로" 참고)
├── public/               # 정적 파일 폴더 (현재 비어 있음)
└── dist/                 # npm run build 실행 시 생성되는 배포용 결과물
```

## ② 각 파일의 역할

| 파일 | 역할 |
|---|---|
| `index.html` | 페이지의 정적 뼈대(header, stage 컨테이너, modal, listmap, footer)와 Tailwind CDN, Google Fonts, CSS/JS 연결 |
| `src/css/style.css` | 전체 디자인(색상 변수, 카드/버튼/모달/리스트맵 스타일, 애니메이션 등) |
| `src/js/main.js` | 데이터(DATA, COMPANY_OVERRIDES 등), 렌더링 로직(단계별 화면), 검색, 리스트맵, 모달, 히스토리(뒤로가기), 이벤트 위임 등 전체 인터랙션 |
| `package.json` | npm 스크립트(`dev`, `build`, `preview`) 및 Vite 의존성 정의 |
| `vite.config.js` | Vite 빌드 설정 (출력 폴더: `dist`) |

## ③ 실행 방법 (개발 서버)

```bash
npm install
npm run dev
```

터미널에 표시되는 로컬 주소(기본 `http://localhost:5173`)를 브라우저에서 열면 됩니다.

## ④ 빌드 방법 (배포용)

```bash
npm run build
```

`dist/` 폴더에 배포 가능한 정적 파일(HTML, 해시가 붙은 CSS/JS)이 생성됩니다.
결과물을 미리 확인하려면:

```bash
npm run preview
```

## ⑤ 기존 기능 중 변경된 부분

**기존 기능 변경 없음.**

- `<style>` 내용을 `src/css/style.css`로, `<script>` 내용을 `src/js/main.js`로 **바이트 단위로 동일하게** 옮겼습니다 (내용 자체는 한 글자도 수정하지 않았습니다).
- 클릭/탭/메뉴/팝업/모달/리스트맵/검색/뒤로가기(History API)/hover/애니메이션 등 모든 인터랙션은 `data-action` 속성 기반 이벤트 위임 방식으로 구현되어 있어 전역 함수에 의존하지 않으므로, `<script type="module">`로 옮겨도 동일하게 동작합니다. (인라인 `onclick=` 같은 속성은 원본에 없었습니다.)
- Tailwind CDN, Google Fonts 링크는 원본 그대로 `<head>`에 유지했습니다.
- `npm install`, `npm run dev`, `npm run build` 모두 정상 동작을 확인했습니다 (콘솔/빌드 오류 없음).

## ⑥ 이미지 경로

현재 프로젝트에서 사용하는 모든 이미지는 **GitHub의 외부 URL**(`https://raw.githubusercontent.com/nanaririri/ai/main/...`)로 연결되어 있습니다. 상대경로나 Base64 이미지는 없습니다.

발견된 이미지 목록:

- `logo.png` (헤더 로고)
- `burim_logo.png` (업체 로고 오버라이드)
- `yu01.jpg`, `yu02.png`, `yu03.jpg`, `yu04.jpg` (서브 카테고리 이미지)
- `ago02.jpg` (서브 카테고리 이미지)
- `material.jpg`, `glass.png` (서브 카테고리 이미지)
- `01.png` (제품 이미지 오버라이드)

요청하신 대로 **모든 GitHub 이미지 URL을 그대로 유지**했으며, 다운로드하거나 다른 이미지로 교체하지 않았습니다. 현재 URL들은 모두 정상적으로 접근 가능한 형태(raw.githubusercontent.com)이므로 별도 조치가 필요하지 않습니다. 다만 GitHub 저장소가 비공개로 전환되거나 파일이 삭제되면 이미지가 깨질 수 있으니, 장기적으로는 `src/assets/images/`로 이미지를 내려받아 로컬 경로로 전환하는 것을 고려해볼 수 있습니다 (현재는 요청에 따라 변경하지 않았습니다).

## 검색 문구 수정 · 노출 순서 변경 · AGO 카탈로그 등록 (28차 업데이트)

**1. 검색창 문구**: "업체명, 카테고리를 검색해보세요.(예: 도어, 한솔도어)"로 정확히 수정했습니다.

**2. 에프에스 빌트인**: 상품 > 가전기기 목록에서 1번으로 노출되도록 순서를 변경했습니다.

**3. 워너홈**: 노출 위치를 시공 > 건축 시공에서 **시공 > 가구·인테리어 시공 > 가구시공**으로 옮기고, 해당 그룹에서 1번으로 노출되도록 했습니다.

**4. AGO 카탈로그**: 첨부하신 실제 브로슈어 PDF(약 6.4MB)를 등록했습니다. AGO 상세 모달의 "카탈로그 다운로드" 버튼을 누르면 이제 더미 텍스트 파일이 아니라 실제 PDF가 다운로드됩니다.

- 수정 파일: `src/js/main.js` (검색창 placeholder, `DATA` 내 업체 순서/위치, `CATALOG_FILE_OVERRIDES`에 AGO 추가), `public/catalogs/`(PDF 신규 추가)
- **부수적으로 발견해 함께 고친 버그**: 카탈로그 다운로드 파일명을 만들 때 URL을 `/`로 잘라 마지막 조각을 쓰는 방식이었는데, 단일 HTML 파일처럼 이미지를 base64로 통째로 내장한 경우 그 안에 `/` 문자가 매우 많이 섞여 있어 파일명이 깨지는 문제가 있었습니다 (예: `AGO_catalog.pdf`가 되어야 할 파일명이 base64 데이터 조각으로 저장됨). 업체명 기반으로 안전하게 파일명을 만들도록 고쳤습니다. 이 버그는 방금 추가한 AGO 카탈로그 때문에 처음 발견됐지만, 이전부터 있던 부림테크 카탈로그의 단일 HTML 버전에도 동일하게 적용되어 있던 문제라 함께 수정했습니다.
- Playwright로 4가지 요청사항과 더불어, 개발용 프로젝트·단일 HTML 파일 양쪽에서 AGO와 부림테크 카탈로그가 모두 정확한 파일명으로 정상 다운로드되는지 확인했고 콘솔 에러는 없습니다.

## 업체 로고 3건 적용 (27차 업데이트)

92 scent, 에프에스 빌트인, 일흥건영 각 업체 상세 모달에 첨부하신 로고를 적용했습니다.

- 수정 파일: `src/js/main.js` (`LOGO_IMAGE_OVERRIDES`에 3건 추가), `public/images/`(로고 3장 추가)
- 기존 AGO·부림테크 로고와 동일한 방식(업체명 기준 오버라이드)으로 처리했습니다.
- Playwright로 3개 업체 모두 로고가 정상 로드되는지(naturalWidth 확인) 검증했고 콘솔 에러는 없습니다.

## 워너홈 취급 제품 이미지 적용 (26차 업데이트)

첨부하신 4장의 실제 시공 현장 사진을 워너홈의 취급 제품 이미지로 적용했습니다. 지난번 설정한 "이미지만 표시, 텍스트 숨김" 옵션은 그대로 유지되어 스펙 텍스트 없이 사진만 표시됩니다.

- 수정 파일: `src/js/main.js` (`PRODUCT_IMAGE_OVERRIDES`에 워너홈 항목 추가), `public/images/`(사진 4장 추가, PNG/JPG 최적화)
- Playwright로 4장 모두 실제 이미지로 정상 로드되는지(naturalWidth 확인), 텍스트가 계속 숨겨져 있는지 확인했고 콘솔 에러는 없습니다.

## 소품 순서 변경 · 에프에스 빌트인 제품 이미지 적용 (25차 업데이트)

**1. 순서 변경**: 가구·인테리어 > 가구·리빙에서 소품 탭을 맨 앞으로 옮겼습니다 (전체 → 소품 → 가구부품 및 하드웨어).

**2. 제품 이미지 적용**: 에프에스 빌트인의 취급 제품 4개에 첨부하신 실제 사진을 용도명 기준으로 정확히 매칭해 적용했습니다 (냉장고/인덕션/시스템에어컨/식기세척기 각각).

- 수정 파일: `src/js/main.js` (`DATA.interior` 소품 그룹 순서, `PRODUCT_IMAGE_OVERRIDES`에 에프에스 빌트인 항목 추가), `public/images/`(제품 사진 4장 추가, PNG→JPEG 최적화)
- Playwright로 탭 순서와 4개 제품 이미지가 각각 올바른 용도에 매칭되어 정상 로드되는지 확인했고 콘솔 에러는 없습니다.

## 워너홈 취급 제품 텍스트 제거 (24차 업데이트)

워너홈의 취급 제품 카드에서 이미지만 남기고, 용도·소재·색상·규격·무게 텍스트를 모두 제거했습니다. 다른 업체는 영향 없이 기존처럼 텍스트가 정상 표시됩니다.

- 수정 파일: `src/js/main.js` (`COMPANY_OVERRIDES`에 `hideProductSpecs` 플래그 추가, 모달의 제품 스펙 렌더링에 조건부 처리 추가)
- Playwright로 워너홈은 텍스트 없이 이미지만 남고, AGO 등 다른 업체는 기존처럼 5개 스펙이 정상 표시되는지(회귀 없음) 확인했고 콘솔 에러는 없습니다.

## 업체 정보 수정 · 신규 업체 추가 (23차 업데이트)

**1. 일흥건영**: 가격대를 중가형 → **저가형**으로 수정했습니다.

**2. 에프에스 빌트인**: 대표제품을 "삼성전자, 빌트인, 가전, 유통" → **"삼성전자 빌트인 가전 유통"**(쉼표 제거)으로 수정했습니다.

**3. 신규 업체 추가**: 시공 > 건축 시공에 **워너홈**을 추가했습니다. 입력하신 정보를 모두 반영했으며, E-mail·홈페이지 값이 "-"(정보 없음)인 경우 깨진 링크가 뜨지 않도록 일반 텍스트로 표시되게 처리했습니다 (다른 업체는 기존처럼 클릭 가능한 링크로 정상 표시).

- 수정 파일: `src/js/main.js` (`co()` tier 값 2건, `COMPANY_OVERRIDES` 항목 추가/수정, 신규 업체 데이터, 모달의 E-mail·홈페이지 렌더링에 "-" 값 예외 처리 추가)
- Playwright로 3개 업체 모두 상세 정보가 정확히 반영됐는지, 워너홈의 "-" 값이 깨지지 않고 정상 표시되는지 확인했고 콘솔 에러는 없습니다.

## 건축 카테고리 순서 · 업체 정보 3건 반영 (22차 업데이트)

**1. 흡음재 순서 변경**: 건축 > 건축마감 안의 소분류 탭 순서에서 흡음재를 1번(맨 앞)으로 옮겼습니다. (전체 → 흡음재 → 단열재 → 도료 → 방수재 → 복합코팅 → 블록 → 유리)

**2. 일흥건영 정보 수정**: 업체 등록 여부(미등록), 가격대(중가형 – 기존과 동일), 제조국(국산), 대표자(전명호), 대표전화(031-942-7031)를 반영했습니다. 취급 제품 4개 모두 동일한 스펙(용도: 실내 마감재 / 소재: 자작합판 / 색상: 우드지정 (판넬당 루버수 15개) / 규격: W600xH21xD2400mm / 무게: 10kg)으로 교체했습니다.
- "업체 등록 여부"를 실제 값과 무관하게 지정할 수 있도록 오버라이드 구조를 확장했습니다 (지금까지는 예시 데이터 등록 여부에 따라 자동 계산됐습니다).

**3. 신규 업체 추가**: 상품 > 가전기기에 **에프에스 빌트인**을 추가했습니다. 주소·등록여부·가격대·대표제품·업태·제조국·대표자·전화·이메일·홈페이지와 취급 제품 4개(냉장고/인덕션/시스템에어컨/식기세척기) 스펙을 모두 입력하신 그대로 반영했습니다.

- 수정 파일: `src/js/main.js` (`DATA.construction` 소분류 순서, `COMPANY_OVERRIDES`, `PRODUCT_SPEC_OVERRIDES`, 오버라이드 적용 로직에 `registered` 필드 추가)
- Playwright로 흡음재 탭 순서, 일흥건영의 6개 필드·제품 4개, 신규 업체의 검색 결과 경로("상품 · 가전기기")·정보 11개 항목·제품 4개가 모두 정확히 반영됐는지 확인했고 콘솔 에러는 없습니다.

## 92 scent 업체 정보 · 제품 스펙 수정 (21차 업데이트)

**업체 정보**: "브랜드명" 바로 아래에 **업체명**이라는 새 항목을 추가해 "에이쿠드"를 표시했습니다 (이 항목이 없는 다른 업체에는 표시되지 않습니다). 제조국(국산), 대표자(박다원), 대표전화(010-2056-7004), E-mail(acud2024@naver.com)도 함께 반영했습니다.

**취급 제품 4개**: 용도(디퓨져)·소재(액체용액)·규격(W100xH100xD140mm)만 표시하고, **색상과 무게는 요청하신 대로 항목 자체를 생략**했습니다. 값이 없을 때는 빈 줄이 아니라 그 항목이 아예 나타나지 않도록 렌더링 방식을 조건부로 바꿨습니다 (다른 업체는 기존처럼 5개 항목 모두 정상 표시됩니다).

- 수정 파일: `src/js/main.js` (`COMPANY_OVERRIDES`에 `companyName` 필드 추가 및 적용 로직 확장, 모달 템플릿에 조건부 "업체명" 행 추가, `PRODUCT_SPEC_OVERRIDES`에 92 scent 항목 추가, 제품 스펙 5개 행 모두 값이 있을 때만 렌더링하도록 수정)
- Playwright로 92 scent의 업체 정보·제품 스펙이 정확히 반영됐는지, 다른 업체(AGO)는 기존처럼 5개 스펙과 업체명 없음 상태가 그대로 유지되는지(회귀 없음) 확인했고 콘솔 에러는 없습니다.

## AGO 업체 정보 · 취급 제품 스펙 수정 (20차 업데이트)

**업체 정보**: 제조국(수입), 대표자(이우복), 대표전화(02-2268-0568)를 반영했습니다. 이 필드들은 지금까지 예시 데이터로 자동 생성되던 항목이었는데, `COMPANY_OVERRIDES`에 실제 정보를 직접 지정할 수 있도록 구조를 확장했습니다.

**취급 제품 스펙**: AGO의 실제 제품 4개(펜던트 조명) 각각의 용도·소재·색상·규격·무게를 정확히 반영했습니다. 이를 위해 `PRODUCT_SPEC_OVERRIDES`라는 새 오버라이드 맵을 추가해, 특정 업체의 특정 제품에는 예시 스펙 대신 실제 스펙이 표시되도록 했습니다 (다른 업체는 기존처럼 예시 스펙 그대로 유지).

- 수정 파일: `src/js/main.js` (`COMPANY_OVERRIDES`에 AGO 항목 확장, 오버라이드 적용 로직에 4개 필드 추가, `PRODUCT_SPEC_OVERRIDES` 신설 및 렌더링 연결)
- Playwright로 상세 모달의 업체 정보 6개 항목과 제품 4개의 스펙이 요청하신 값과 정확히 일치하는지 확인했고 콘솔 에러는 없습니다.

## 가구·인테리어 2뎁스 순서 변경 (19차 업데이트)

요청하신 순서대로 가구·인테리어의 2뎁스 카테고리 순서를 재배열했습니다.

- 새 순서: 가구·리빙 → 주방 → 오피스 → 바스 → 도어·중문 → 바닥재 → 석재 → 벽장재 → 창호 → 커튼 → 원자재·부자재
- 요청 목록에 없었던 **금속**은 삭제하지 않고 맨 뒤에 그대로 유지했습니다.
- 업체 데이터·이미지·아이콘 등 각 카테고리의 내용은 전혀 변경하지 않고 순서만 바꿨습니다.
- 수정 파일: `src/js/main.js` (`DATA.interior.subs` 배열 순서만 재배치)
- Playwright로 화면에 렌더링되는 순서가 요청하신 순서와 정확히 일치하는지 확인했습니다.

## AGO 업체 로고 적용 (18차 업데이트)

- 첨부하신 로고 이미지를 **AGO** 업체 상세 모달의 로고 표시 영역에 적용했습니다.
- 수정 파일: `src/js/main.js` (`LOGO_IMAGE_OVERRIDES` 맵에 한 줄 추가), `public/images/`(로고 이미지 1장 추가)
- 기존 부림테크 로고와 동일한 방식(업체명 기준 오버라이드)으로 처리했습니다.
- Playwright로 검색 후 상세 모달에서 로고가 정상 로드되는지(naturalWidth 확인) 검증했고 콘솔 에러는 없습니다.

## 이미지 재배치 4건 (17차 업데이트)

**[건축]**
- 외장재에 쓰이던 기존 사진(`material.jpg`)을 **건축마감**으로 옮겼습니다.
- 외장재에는 새로 첨부하신 목재 클래딩 외관 사진을 적용했습니다 (PNG→JPEG 최적화, 375KB→73KB).

**[상품]**
- 일반상품 이미지를 **상품 1뎁스 카드와 동일한 사진**(`hero-product.png`)으로 교체했습니다.
- 기타 이미지에 **워크업 취급 제품 4번째 사진**(`wo04.JPG`)을 적용했습니다.

- 수정 파일: `src/js/main.js`(`SUB_IMAGES` 맵), `public/images/`(외장재 신규 이미지 1장 추가)
- Playwright로 4곳 모두 의도한 이미지로 정상 교체됐는지 src 경로와 실제 로드 여부(naturalWidth)를 확인했고 콘솔 에러는 없습니다.

## 2뎁스 이미지 8장 적용 · 상품 카테고리 3개로 정리 (16차 업데이트)

**[가구·인테리어]** 금속 → `금속.jpg` 적용

**[건축]** 야외 바닥재 → `야외바닥재.png` / 건축설계 → `건축설계.png` / 모듈러하우스 → `모듈러하우스.png`(기존 GitHub 이미지에서 교체) / 조형물 → `조형물.png` 적용

**[상품]** 2뎁스를 요청하신 대로 **가전기기 · 일반상품 · 기타** 3개만 남기고 기존 임시 항목(인기상품/신상품/추천상품)은 제거했습니다. 가전기기 → `가전기기.png`, 일반상품 → `일반상품.png` 적용

**[시공]** 건축 시공 → `건축시공.JPG` 적용 (지난번 이미지가 없었던 항목에 이제 채워졌습니다)

- 수정 파일: `src/js/main.js`(`SUB_IMAGES` 맵, `product.subs` 배열), `public/images/`(이미지 8장 추가)
- Playwright로 8개 카드 모두 실제 이미지가 정상 로드되는지(naturalWidth·HTTP 200 확인), 상품 카테고리가 정확히 3개만 남았는지 확인했고 콘솔 에러는 없습니다.

## 1뎁스별 카테고리 재구성 (15차 업데이트)

**[가구·인테리어]**
- **금속** 중분류를 신규 추가했습니다 (금속자재 그룹, 예시 업체 3곳).

**[건축]**
- **건축시공** → **건축마감**으로 이름만 변경했습니다 (하위 업체·소분류는 그대로).
- **시공**(한빛종합건설, 대성시공) → 1뎁스 **시공** 카테고리로 이동, 중분류 타이틀을 **"건축 시공"**으로 변경했습니다.
- **기타**(워크업, 종합자재상사) → 1뎁스 **상품** 카테고리로 이동했습니다 (이름 유지).

**[상품]**
- **가전기기**, **일반상품** 중분류를 신규 추가했습니다 (각각 예시 업체 2~3곳).
- 건축에서 이동해 온 **기타**도 함께 포함됩니다.

**[시공]**
- 기존에 개별 중분류 6개(가구시공/주방시공/바스시공/오피스시공/기본공사/철거)로 흩어져 있던 것을 **"가구·인테리어 시공"** 중분류 하나로 묶고, 그 안의 6개 소분류로 재구성했습니다. 업체 데이터는 전부 그대로 유지했습니다.
- 건축에서 이동해 온 시공 업체들은 **"건축 시공"** 중분류로 별도 구성했습니다.
- **이미지**: "가구·인테리어 시공"에는 예전에 같은 성격의 카드에 쓰였던 사진(`installation.jpg`)을 그대로 이어서 적용했습니다. **"건축 시공"은 이전에도 지정된 이미지가 없어서 이번에도 아이콘으로 대체 표시됩니다** — 원하시는 사진이 있으면 알려주시면 바로 적용해 드리겠습니다.

수정 파일: `src/js/main.js`(`DATA` 구조 전체 재편, `SUB_IMAGES` 맵 조정) — 다른 파일은 변경하지 않았습니다.

Playwright로 4개 1뎁스 카테고리의 하위 목록이 모두 정확히 재구성됐는지, 이동된 업체(한빛종합건설·워크업·가구시공전문코리아 등)가 검색에서 새 경로로 정상 노출되는지, 이미지가 의도대로 이어졌는지, 콘솔 에러 여부까지 확인했습니다.

## 1뎁스 타이틀 크기 소폭 확대 (14차 업데이트)

이전에 40% 줄였던 1뎁스 카드 타이틀이 너무 작다는 피드백에 따라 약간 키웠습니다.

- `text-[21.6px] md:text-[28.8px]` → `text-[26px] md:text-[35px]`
- 4개 카드(가구·인테리어/건축/상품/시공) 모두 동일하게 적용
- 수정 파일: `src/js/main.js` (`heroCard()`의 `<h2>` 클래스 한 줄만 변경)

## 1뎁스 타이틀 크기 40% 축소 (13차 업데이트)

1뎁스 카드(가구·인테리어 / 건축 / 상품 / 시공)에 표시되는 타이틀 글자 크기를 40% 줄였습니다.

- `text-4xl md:text-5xl`(36px / 48px) → `text-[21.6px] md:text-[28.8px]`(원래 크기의 60%)
- 4개 카드 모두 동일하게 적용되며, 다른 텍스트·레이아웃·색상은 변경하지 않았습니다.
- 수정 파일: `src/js/main.js` (`heroCard()` 함수의 `<h2>` 태그 클래스 한 줄만 변경)
- Tailwind 유틸리티를 실제 값으로 주입해 렌더링된 `font-size`가 정확히 28.8px(데스크톱 기준)로 나오는지 측정해 확인했습니다.

## 사이트명 변경 · 1뎁스 구조 4개로 재편 (12차 업데이트)

- **사이트명**: "인테리어 건축 라이브러리" → **"협력업체 통합 라이브러리"**로 변경 (헤더 로고 옆 텍스트, `<title>` 태그)
- **1뎁스(대분류) 4개로 재편**: 기존 2개(인테리어/건축) → **가구·인테리어, 건축, 상품, 시공** 4개
  - **가구·인테리어**: 기존 "인테리어"를 이름만 변경 (하위 11개 중분류는 그대로, "시공"만 아래로 분리)
  - **건축**: 변경 없음
  - **상품** (신규): 인기상품·신상품·추천상품 3개 중분류, 예시 업체 배치 — 실제로 어떤 하위 카테고리를 원하시는지 구체적으로 알려주시면 그에 맞게 다시 구성해 드리겠습니다. 지금은 임시 placeholder입니다.
  - **시공** (기존 "가구·인테리어 > 시공"을 1뎁스로 승격): 가구시공·주방시공·바스시공·오피스시공·기본공사·철거 6개 중분류를 그대로 유지 (업체 데이터도 전부 유지, 계층만 한 단계 위로 이동)
- **1뎁스 히어로 이미지**: 상품(`상품.png`), 시공(`시공.jpg`) 첨부 이미지를 각각 카드 배경으로 적용. 기존 가구·인테리어/건축 이미지는 그대로 유지.
- 수정 파일: `src/js/main.js`(`DATA` 구조, `HERO_IMAGES` 맵 신설), `index.html`(제목/헤더 텍스트), `public/images/`(신규 이미지 2장 추가)
- Playwright로 1뎁스 카드 4개, 각 카드의 배경 이미지, 가구·인테리어에서 시공이 빠진 것, 시공/상품 1뎁스의 하위 카테고리, 이동된 업체가 검색에서 새 경로(시공 > 가구시공)로 정상 노출되는지, 콘솔 에러 여부까지 확인했습니다.

## 인테리어 2뎁스 이미지 적용 (11차 업데이트)

첨부해 주신 4장의 사진을 인테리어 2뎁스(중분류 카드) 이미지로 적용했습니다: **원자재·부자재, 커튼, 오피스, 시공**.

- 업로드하신 이미지를 `public/images/` 폴더에 저장했습니다 (`raw-material.jpg`, `curtain.png`, `office.jpg`, `installation.jpg`).
- Vite의 `public/` 폴더는 빌드 시 그대로 `dist/`에 복사되므로, 기존 GitHub 외부 URL 이미지들과 동일한 방식(`SUB_IMAGES` 맵)으로 함께 관리되며 개발 서버(`npm run dev`)와 배포 빌드(`npm run build`) 모두에서 정상 동작합니다.
- 이 "시공"은 **인테리어 > 시공**(id: `installation`) 카드용입니다. 건축 대분류에도 이름이 같은 "시공"(id: `construction-service`) 카드가 별도로 있는데, 그쪽은 요청하신 범위가 아니라서 기존 상태(아이콘 표시) 그대로 두었습니다.
- 수정 파일: `src/js/main.js`(`SUB_IMAGES` 맵에 4줄 추가), `public/images/`(이미지 4개 추가) — 다른 코드는 변경하지 않았습니다.
- Playwright로 인테리어 카테고리 화면에서 4개 카드 모두 실제 이미지가 깨지지 않고 로드되는지(naturalWidth 확인) 검증했고, 콘솔 에러도 없습니다.

## 규격 텍스트 줄바꿈 정렬 수정 (10차 업데이트)

취급 제품의 "규격" 값이 길어서 줄바꿈될 때, 두 번째 줄이 라벨("규격") 아래(맨 왼쪽)로 내려가지 않고 규격값이 시작하는 위치에 맞춰 정렬되도록 수정했습니다.

- `규격` 항목만 `<div>` 한 줄을 `flex items-start gap-1`로, 라벨은 `shrink-0`, 값은 `min-w-0 flex-1`로 바꿔 값 텍스트가 자신의 flex 박스 안에서만 줄바꿈되도록 했습니다.
- 색상(`var(--gray)`, `var(--ink)`), 폰트, 글자 크기는 전혀 변경하지 않았고, 라벨-값 사이 간격도 기존과 거의 동일하게(`gap-1`) 유지했습니다.
- 용도/소재/색상/무게 등 다른 항목과 전체 레이아웃은 건드리지 않고 `규격` 한 줄만 수정했습니다.
- 수정 파일: `src/js/main.js` (해당 한 줄만 변경)
- 좁은 폭에서 실제로 텍스트가 2줄로 줄바꿈되는지, 두 번째 줄이 값의 시작 위치와 같은 x좌표에서 시작하는지 좌표 기반으로 확인했습니다. (이 샌드박스는 Tailwind CDN을 불러올 수 없어 픽셀 스크린샷 대신, flex 관련 유틸리티의 실제 CSS를 주입해 값 박스의 높이·좌표를 직접 측정하는 방식으로 검증했습니다. flex 아이템 내부 줄바꿈은 브라우저 표준 동작이라 실제 환경에서도 동일하게 작동합니다.)

## PC 버전 업체 레이어팝업 가로 확장 (9차 업데이트)

PC(데스크톱, 1024px 이상) 화면에서 업체 상세 레이어팝업의 가로 폭을 더 넓혔습니다.

- `lg:max-w-3xl`(약 768px) → `lg:max-w-5xl`(약 1024px)
- 모바일·태블릿(1024px 미만) 폭은 기존과 동일하게 유지했습니다.
- 수정 파일: `index.html` (`#modal-panel` 클래스 한 줄만 변경)
- Tailwind 유틸리티의 실제 CSS 값을 주입해 렌더링 폭이 약 768px → 약 1024px로 확장된 것을 직접 측정해 확인했습니다.

## 취급 제품 텍스트 스타일 · 모달 스크롤 구조 개선 (8차 업데이트)

- **취급 제품 정보 텍스트**: 글자 크기를 `text-[11px]` → `text-sm`(14px)로 키우고, 값 텍스트 색상을 옅은 회색(`--ink-soft`)에서 가장 진한 잉크 색상(`--ink`)으로 바꿔 더 또렷하게 보이도록 했습니다. 항목 라벨(용도/소재/색상/규격/무게)은 기존처럼 작은 보조색으로 유지해 라벨과 값이 구분되게 했습니다.
- **모달 스크롤 구조**: 모달 패널을 세로 flex 레이아웃으로 바꿔 `#modal-body`(업체 정보·취급 제품)만 세로 스크롤되고, `#modal-footer`(카탈로그 다운로드 버튼)는 하단에 고정되도록 했습니다.
  - 수정 파일: `index.html`(모달 패널 구조를 `#modal-body` + `#modal-footer`로 분리), `src/js/main.js`(다운로드 버튼을 `#modal-footer`에 별도로 렌더링)
  - Tailwind의 실제 CSS 유틸리티(`flex`, `flex-1`, `overflow-y-auto`, `shrink-0`)를 적용한 시뮬레이션 테스트로, 본문은 스크롤되고 하단 버튼은 위치가 고정되는지 직접 확인했습니다. (이 샌드박스는 네트워크 정책상 Tailwind CDN 자체는 못 불러오므로, 해당 유틸리티의 실제 CSS 규칙을 동일하게 주입해 검증했습니다.)

## 취급 제품 상세 스펙 추가 (7차 업데이트)

취급 제품 각 이미지 아래에 상세 정보를 추가했습니다.

- **용도** / **소재** / **색상** / **규격** / **무게** — 제품 이미지마다 5개 항목이 표시됩니다.
- 실제 사진이 있는 업체(부림테크, 유송타일 등)와 일반 예시 스와치 이미지를 쓰는 업체 모두 동일하게 표시되도록, 제품 배열의 순서(index) 기반으로 값을 생성하는 `productSpecs()` 헬퍼를 추가했습니다.
- 수정 파일: `src/js/main.js` (제품 목록 렌더링 부분에만 추가, 기존 이미지·레이아웃 구조는 그대로 유지)
- Playwright로 사진이 있는 업체/없는 업체 모두 5개 항목이 정상 표시되는지, 콘솔 에러가 없는지 확인했습니다.

## 업체 정보 · 취급 제품 좌우 레이아웃 (6차 업데이트)

업체 상세 모달에서 "업체 정보"와 "취급 제품"을 좌/우로 나란히 배치했습니다.

- 데스크톱(≥1024px, `lg` 브레이크포인트)에서는 좌측에 업체 정보, 우측에 취급 제품이 2단으로 표시됩니다.
- 모바일·태블릿(1024px 미만)에서는 좁은 화면에서 두 칸이 억지로 눌리지 않도록 기존처럼 세로로 쌓이는 레이아웃을 유지했습니다.
- 좌우 2단 레이아웃이 여유 있게 보이도록 모달 패널의 최대 너비를 데스크톱 기준으로 넓혔습니다(`sm:max-w-lg` → `lg:max-w-3xl`, 모바일/태블릿 너비는 기존과 동일).
- "카탈로그 다운로드" 버튼은 두 칸 전체 아래에 기존처럼 가로로 꽉 차게 유지했습니다.
- 수정 파일: `src/js/main.js`(모달 렌더링 구조), `index.html`(모달 패널 최대 너비) — 정보 항목 자체의 내용/문구는 변경하지 않았습니다.
- Playwright로 그리드 구조(업체 정보/취급 제품이 각각 별도 컬럼으로 존재하는지)와 콘솔 에러 여부를 확인했습니다. 다만 이 테스트 환경은 Tailwind CDN을 네트워크 정책상 불러올 수 없어 실제 픽셀 단위 시각 렌더링까지는 확인하지 못했습니다 — `npm run dev`로 실행해 직접 화면을 확인해 보시길 권장드립니다.

## 업체 정보 필드 추가 (5차 업데이트)

업체 상세 정보(모달)에 아래 항목을 추가했습니다.

- **업태**: 제조 / 유통 중 1
- **제조국**: 국산 / 중국 / 이태리 중 1
- **대표자**: 대표자명
- **대표전화**: `tel:` 링크로 연결해 모바일에서 탭하면 바로 전화 연결
- **E-mail**: `mailto:` 링크로 연결해 탭하면 바로 메일 작성

표시 위치는 기존 "대표제품" 항목 바로 아래이며, 다운로드되는 카탈로그 텍스트 파일에도 동일하게 포함했습니다.

- 수정 파일: `src/js/main.js` (`co()` 헬퍼 함수에 필드 생성 로직 추가, 모달/카탈로그 렌더링에 항목 추가)
- 전화번호는 02/031/032/041/051/053/062/063 등 실제 존재하는 지역번호 형식으로 생성해 `tel:` 링크가 유효하도록 했습니다.
- 데스크톱·모바일(터치) 환경 모두에서 전화 링크와 이메일 링크가 정상적으로 렌더링되는지 Playwright로 확인했습니다.

## 카테고리 추가 (4차 업데이트)

인테리어 대분류에 **시공** 중분류를 새로 추가했습니다. (건축 대분류에 이미 있는 "시공"과 이름은 같지만 별도 항목이며, 내부 id를 `installation`으로 구분해 건축의 `construction-service`와 충돌하지 않도록 했습니다.)

- 인테리어 > 시공 > 소분류: 가구시공, 주방시공, 바스시공, 오피스시공, 기본공사, 철거 (각 2개씩 예시 업체 배치, 총 12개 업체)
- 수정 파일: `src/js/main.js` (`DATA.interior.subs`에 항목 추가만 했고, 기존 카테고리·업체 데이터는 전혀 건드리지 않았습니다)
- Playwright로 인테리어 카테고리 목록에 "시공"이 정확히 추가됐는지, 6개 소분류 탭이 모두 뜨는지, 탭별로 업체가 정상 표시되는지, 콘솔 에러가 없는지 확인했습니다.

## 카테고리 구조 변경 (3차 업데이트)

첨부하신 대분류/중분류/소분류 이미지에 맞춰 전체 카테고리 체계를 재구성했습니다.

- 수정 파일: `src/js/main.js` (`DATA` 객체, 아이콘, 이미지 매핑), 다른 파일은 변경 없음
- 대분류: **인테리어**(11개 중분류) / **건축**(9개 중분류, 기존 "외장 건축 기자재"에서 이미지에 맞춰 명칭 변경)
- 인테리어 중분류: 원자재·부자재, 가구·리빙, 주방, 바스, 도어·중문, 바닥재, 석재, 벽장재, 창호, 커튼, 오피스
- 건축 중분류: 건축시공, 야외 바닥재, 외장재, 조형물, 야외가구, 건축설계, 모듈러하우스, 시공, 기타
- 각 중분류 하위 소분류는 이미지에 표기된 항목만 반영했고(빈 칸은 의미 없는 셀 병합으로 보고 제외), 각 소분류에는 최소 1개 이상의 예시 업체를 배치해 화면이 비어 보이지 않도록 했습니다.
- 기존에 등록되어 있던 실제 예시 업체(부림테크, 유송타일, 아주스톤, 포스코휴먼스, 일흥건영, 미가월패널, AGO, 92 scent, SITE LESS, 워크업 등)는 삭제하지 않고 의미가 가장 가까운 새 소분류로 재배치했습니다. (예: 일흥건영 → 건축 > 건축시공 > 흡음재, SITE LESS → 건축 > 건축설계, 워크업 → 건축 > 기타)
- 새로운 중분류(원자재·부자재, 가구·리빙 소품, 오피스, 커튼, 조형물, 건축설계, 모듈러하우스, 시공 등)에는 예시 업체를 새로 추가했습니다.
- **부수적으로 발견해 함께 고친 버그**: 카테고리 이미지 로딩 실패 시 아이콘으로 대체하는 `handleSubImgError` 함수가 `<script type="module">` 환경에서는 전역 스코프에 노출되지 않아, 이미지가 로드되지 않을 경우 콘솔 에러가 발생하며 대체 아이콘이 표시되지 않는 문제가 있었습니다(1차 구조 분리 작업 시 생긴 잠재적 버그). `window.handleSubImgError`로 명시적으로 등록해 수정했습니다.
- Playwright로 인테리어 11개·건축 9개 중분류가 이미지와 정확히 일치하는지, 소분류 필터 탭이 올바르게 뜨는지, 재배치된 업체(부림테크 등)가 검색에서 새 카테고리로 정상 노출되는지, 콘솔 에러가 없는지 모두 확인했습니다.

## 음성 검색 기능 (2차 업데이트)

기존 검색창(1단계 화면)에 **Web Speech API(SpeechRecognition) 기반 음성 검색**을 추가했습니다.

- 수정 파일: `src/js/main.js` (검색창 HTML 마크업 + 음성 인식 로직), `src/css/style.css` (마이크 버튼 스타일만 추가)
- 기존 검색 로직(`performSearch`)을 그대로 재사용하며, 별도의 검색 로직을 새로 만들지 않았습니다.
- 마이크 버튼은 검색창 안, 지우기(×) 버튼과 검색 버튼 사이에 추가되었고 최소 44×44px 터치 영역을 확보했습니다.
- `SpeechRecognition`/`webkitSpeechRecognition`을 지원하지 않는 브라우저에서는 마이크 버튼이 자동으로 숨김 처리되어 기존 검색 기능에 전혀 영향을 주지 않습니다.
- 언어는 `ko-KR`, `interimResults: true`로 설정되어 있어 말하는 동안 검색창 텍스트가 실시간으로 갱신됩니다. 최종 인식 결과가 나오면 기존 검색(`performSearch`)이 그대로 실행됩니다.
- 오류(권한 거부, 미지원, 무음, 네트워크 오류 등)는 기존 검색 결과 드롭다운(`#search-results`) 영역을 재사용해 안내 문구로 표시하며, 어떤 오류가 나도 기존 텍스트 검색은 정상 동작합니다.
- 접근성: 마이크 버튼에 `aria-label="음성 검색"`, `aria-pressed`를 추가했고, 화면에는 보이지 않는 `aria-live` 영역으로 상태 변화를 스크린리더에 안내합니다.
- 1단계 화면을 벗어나거나 `Esc`를 누르면 진행 중인 음성 인식을 자동으로 중단합니다.
- Playwright로 마이크 클릭 → 중간 결과 반영 → 최종 결과로 기존 검색 실행 → 오류 처리 → 미지원 브라우저 시 자동 숨김까지 모두 시뮬레이션하여 정상 동작을 확인했습니다.

## 기술 스택

- **Vanilla JavaScript** (프레임워크 없음 — 원본이 프레임워크 없이 구현되어 있어 그대로 유지)
- **Vite** — 개발 서버 + 프로덕션 빌드
- **Tailwind CSS (CDN)** — 원본과 동일하게 CDN 스크립트로 로드
